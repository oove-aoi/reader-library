前端畫面當前預定項目:
思考前端畫面應該長什麼樣子
先建純作者頁面跟作者們list的畫面
後端還要加上輸出所有作者列表的相關方法(使用名稱查詢時可能也要改成回傳列表)

實際操作下來各種卡手
書籍部分新增要先填作者跟系列，也許可考慮在書籍新增時前端跳出是否新增作者跟系列的選項?
系列可能好解決一點，預設使用書名
作者還要再考慮下

5/4
前端更早之前已修正，可正常呼叫後端API路徑

但是遇到後端因為沒有傳Access-Control-Allow-Origin而導致Cors問題
已在後端做修改

嘗試將原先寫死的作者api路徑改成獲取網址的動態參數
1.修改AuthorRouter.ts中的路徑path: '/authors/:id',，確保有動態參數可以傳入
2.在component 組件中獲取該參數
(Number(route.params.id)->因為url是字串，所以為了配合getAuthorById()而用上Number轉型)

然後發現新問題，輸入不存在的值會因為沒有相應的處理邏輯而停在載入中
調整script部分跟畫面讓他們可以處理沒有找到的狀況

順便調了下畫面

"""
##重點預定

產生作者列表頁(後端有使用分頁查詢)
在作者細節頁面增加更新與刪除按鈕
新增查詢用介面

思考前端畫面應該長什麼樣子

實際操作下來各種卡手
書籍部分新增要先填作者跟系列，也許可考慮在書籍新增時前端跳出是否新增作者跟系列的選項?
系列可能好解決一點，預設使用書名
作者還要再考慮下

追蹤的部分
也許可以在系列新增的時候就詢問是否添加進追蹤中?

而且直到現在系列跟系列追蹤的id還是再亂跳...
還有考慮是否要給翻譯作品添加原書名的欄位

理解typescript的寫法跟一般JS的差異(目前向axios的串接部分可能雷就挺大的)


並加上前端vue.js(http://localhost:8080/v3/api-docs
使用該連結的openapi.json 搭配 openapi generator、vue來產出一個前端畫面)


"""
5/5
